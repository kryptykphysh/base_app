services:
  app:
    build:
      context: .
      dockerfile: Dockerfile-dev
    command: ./bin/dev
    image: base-app
    ports:
      - "3000:3000"
    volumes:
      - .:/rails
  guard:
    build:
      context: .
      dockerfile: Dockerfile-dev
    command: bundle exec guard -i
    image: base-app
    volumes:
      - .:/rails
